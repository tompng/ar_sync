#!/usr/bin/env ruby

require 'bundler/setup'
require 'ar_sync'
require 'pry'
require_relative '../test/model'
ArSync.on_update do |key, patch|
  msg = [key, patch].inspect
  puts "\e[1m#{msg}\e[m"
end

Pry.start
